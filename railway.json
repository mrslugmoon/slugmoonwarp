{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/"
  }
}
